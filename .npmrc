# NPM Configuration for Theme Switcher Extension

# Ensure proper package resolution
legacy-peer-deps=false

# Use exact versions for dependencies
save-exact=false

# Audit settings
audit=true
audit-level=moderate
